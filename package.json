{
  "name": "ai-scanner",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node generate-images-map.cjs && vite",
    "build": "node generate-images-map.cjs && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.4"
  },
  "dependencies": {
    "@vladmandic/face-api": "^1.7.15"
  }
}
